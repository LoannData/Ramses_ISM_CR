diff a/pm/sink_particle_tracer.f90 b/pm/sink_particle_tracer.f90	(rejected hunks)
@@ -358,9 +358,10 @@ contains
           if (ok) then
              igrid = cell2grid(icell)
              ok = (grid_level(igrid) == ilevel)
+             print*, 'ok?', myid, '->', icpu, ilevel, grid_level(igrid), ok
           end if
           
           if (ok) then
              ! Loop on the particles in the grid
              ipart = headp(igrid)
              do i = 1, numbp(igrid)
